{
  "name": "@skarion/worker-cron",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "echo \"(stub) no build yet for @skarion/worker-cron\"",
    "dev": "echo \"(stub) no dev yet for @skarion/worker-cron\"",
    "test": "echo \"(stub) no tests yet for @skarion/worker-cron\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
